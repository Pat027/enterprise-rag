{{- if .Values.vllmGuard.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "enterprise-rag.fullname" . }}-vllm-guard
  labels:
    {{- include "enterprise-rag.componentLabels" (dict "ctx" . "component" "vllm-guard") | nindent 4 }}
spec:
  type: {{ .Values.vllmGuard.service.type }}
  ports:
    - name: http
      port: {{ .Values.vllmGuard.service.port }}
      targetPort: 8000
      protocol: TCP
  selector:
    {{- include "enterprise-rag.selectorLabels" (dict "ctx" . "component" "vllm-guard") | nindent 4 }}
{{- end }}
